// parenteses para parametros e {} para fechar o codigo
   
   function vouAprender(){
        console.log ("Por isso estudo para conseguir o que quero alcançar!");
        console.log ("Se eu colocar outro console o que me consola se não aparecer nada");
    }
 
    vouAprender();

    // abaixo outra opção de declarar uma função, vamos lá 
    // as coisas começam a fazer mais sentidos e farei um teste abaixo 

    let divisão = function (numC, numD){
        console.log (numC / numD);
    }

    divisão (15,5);


// teste 
var produtoAtivo = false
if (){
    console.log ('Esse produto não está ativo')
}


